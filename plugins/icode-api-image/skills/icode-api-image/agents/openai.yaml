interface:
  display_name: "Icode API Image"
  short_description: "Generate or edit images through the Midlight relay"
  default_prompt: "Use $icode-api-image to generate or edit an image through my Midlight relay."
policy:
  allow_implicit_invocation: true
